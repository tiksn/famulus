name: Lint

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - name: Super-Linter
      uses: github/super-linter@v6
